# Runtime deps for the project scripts the DAG shells out to (ingestion, feature
# build, model retrain, RAG reindex). Installed into an isolated venv
# (/usr/local/airflow/project_venv) by the Dockerfile so they never perturb the
# Airflow environment. Pinned to the versions verified in the project's local
# .venv. Excludes web/dashboard/dev tooling (streamlit, fastapi, jupyter) that the
# pipeline tasks don't need.
python-dotenv==1.2.2

# Ingestion
nflreadpy==0.1.5
polars==1.42.1
pyarrow==24.0.0

# Data / DB
pandas==3.0.3
numpy==2.4.6
psycopg2-binary==2.9.12
sqlalchemy==2.0.51

# ML (features + train + batch_predict)
xgboost==3.3.0
scikit-learn==1.9.0
shap==0.52.0
joblib==1.5.3

# RAG reindex (rag/embed_store.py)
langchain==1.3.14
langchain-openai==1.4.1
langchain-postgres==0.0.17
langchain-text-splitters==1.1.2
openai==2.52.1
pgvector==0.3.6
tiktoken==0.13.0
