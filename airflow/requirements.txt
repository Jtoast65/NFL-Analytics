# Deps for the Airflow environment itself. Kept minimal and pinned so pip does
# not backtrack against the Astro Runtime's constraint set (an unpinned dbt-core
# here caused pip ResolutionTooDeep). Only the load_to_s3 PythonOperator runs in
# this env, and it needs boto3.
#
# dbt and the project's ingestion/ML/RAG deps are installed into separate
# virtualenvs in the Dockerfile (dbt_venv, project_venv) to avoid dependency
# conflicts with Airflow. See dbt_requirements.txt and project_requirements.txt.
boto3==1.43.68
